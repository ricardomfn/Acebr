<div class="d-flex justify-content-center align-items-center vh-100">
  <div class="container">
    <h1 class="title mb-4 text-center">Entrar</h1>
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email", class: "create-input transparent-input" },
                    label: "E-mail" %>

        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password", class: "create-input transparent-input" },
                    label: "Senha" %>
        <%= f.input :remember_me, as: :boolean,
                    label: "Lembre-me" if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions text-center">
        <%= f.button :submit, "Acessar", class: "btn-ace mt-3 mb-3 w-100" %>
        <%= link_to "Cadastre-se", new_registration_path(resource_name), class: "btn-ace btn mb-3 w-100" %>
        <%= link_to "Esqueci a senha", new_password_path(resource_name), class: "btn-ace btn w-100" %>
      </div>
    <% end %>
  </div>
</div>
